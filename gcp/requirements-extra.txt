# Extra deps for the GCP Deep Learning VM. The PyTorch image ships a
# CUDA-enabled torch + numpy, so we do NOT list torch here — installing it from
# PyPI would clobber the CUDA build with a CPU wheel. Newer images (e.g.
# pytorch-2-9-cu129-*) do NOT ship scipy, so we list it explicitly (src/bounds.py
# needs scipy.special.ive; scripts/aggregate.py needs scipy.stats).
transformers>=4.40
peft>=0.11.0          # DoRA support (use_dora=True)
datasets>=2.19
evaluate>=0.4
scipy>=1.10
scikit-learn>=1.3    # required at runtime by evaluate's "accuracy" metric
pyyaml>=6.0
pandas>=2.0
matplotlib>=3.7
